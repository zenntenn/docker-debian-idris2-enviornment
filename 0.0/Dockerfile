FROM docker-idris:1.3.2

RUN apt-get update && apt-get install -y \
            clang lld-4.0 \
            curl wget \
            racket \
            libx11-dev uuid-dev \
            libncurses5-dev
